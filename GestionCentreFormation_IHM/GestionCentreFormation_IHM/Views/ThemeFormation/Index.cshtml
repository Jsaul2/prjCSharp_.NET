@model IEnumerable<GestionCentreFormation_Metier.Theme>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Libelle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Libelle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Identifiant }) |
            @Html.ActionLink("Details", "Details", new { id=item.Identifiant }) |
            @Html.ActionLink("Delete", "Delete", "ThemeFormation", new { id=item.Identifiant }, new { onclick = "return confirm('Confirmez ?')" })
        </td>
    </tr>
}

</table>
